# Исправление проблемы с таймаутом при сборе месячной статистики

## Описание проблемы

При нажатии на кнопки для получения месячной статистики (`monthly_stats_current`, `monthly_stats_previous`, `month_select`) приложение падало с ошибкой таймаута:

```
TimeoutError: Promise timed out after 90000 milliseconds
```

Сбор статистики за месяц занимает значительное время (более 90 секунд), так как для каждого дня месяца выполняются множественные API-запросы:
- Получение списка маршрутов
- Получение деталей каждого маршрута
- Получение деталей каждого заказа
- Расчет расстояний и заработка

## Причина

Библиотека Telegraf имеет встроенный таймаут в 90 секунд для обработки callback query. Когда обработчик не завершается в течение этого времени, возникает необработанное исключение, которое приводит к падению приложения.

## Решение

Используется асинхронная обработка длительных операций с помощью `setImmediate()`. Это позволяет:

1. Немедленно ответить на callback query
2. Запустить сбор статистики в фоновом режиме
3. Не блокировать event loop
4. Отправить результат пользователю после завершения обработки

### Технические детали

#### Изменения в обработчиках

Изменены три обработчика callback:
- `monthly_stats_current` - статистика за текущий месяц
- `monthly_stats_previous` - статистика за прошлый месяц  
- `month_select_(\d+)_(\d+)` - статистика за выбранный месяц

#### Структура нового обработчика

```javascript
bot.action("handler_name", async (ctx) => {
  // 1. Немедленно отвечаем на callback query
  await ctx.answerCbQuery();
  
  // 2. Получаем необходимые данные
  const userId = ctx.from.id;
  const chatId = ctx.chat.id;
  
  // 3. Проверяем авторизацию
  // ...
  
  // 4. Отправляем начальное сообщение
  await ctx.reply("⏳ Собираю статистику...");
  
  // 5. Запускаем асинхронную обработку
  setImmediate(async () => {
    try {
      // Длительная операция
      const stats = await monthlyStats.collectMonthlyStatistics(...);
      
      // Отправляем результат через bot.telegram.sendMessage
      await bot.telegram.sendMessage(chatId, message, keyboard);
    } catch (error) {
      console.error("Error:", error);
      await bot.telegram.sendMessage(chatId, "❌ Ошибка");
    }
  });
});
```

#### Ключевые изменения

1. **Сохранение `chatId`**: Необходимо для отправки сообщений из асинхронного контекста
2. **Использование `bot.telegram.sendMessage`**: Вместо `ctx.reply`, так как контекст недоступен в `setImmediate`
3. **Информирование пользователя**: Добавлено сообщение о том, что можно продолжить работу с ботом

#### Обработка ошибок

Добавлен глобальный обработчик ошибок для бота:

```javascript
bot.catch((err, ctx) => {
  console.error("Unhandled error while processing", ctx.update);
  console.error("Error:", err);
  
  if (ctx?.reply) {
    ctx.reply("❌ Произошла ошибка при обработке запроса. Попробуйте позже.")
      .catch((e) => console.error("Failed to send error message:", e));
  }
});
```

## Преимущества решения

- **Надежность**: Приложение больше не падает при длительных операциях
- **Отзывчивость**: Пользователь может продолжить работу с ботом во время сбора статистики
- **Информативность**: Пользователь видит прогресс обработки
- **Масштабируемость**: Решение применимо для любых длительных операций

## Альтернативные подходы

### Подход 1: Увеличение таймаута (не рекомендуется)

Можно было бы увеличить таймаут Telegraf, но это:
- Не решает проблему блокировки event loop
- Может привести к проблемам с Telegram API
- Ухудшает user experience

### Подход 2: Кеширование данных

Можно кешировать собранную статистику и обновлять её по расписанию:
- Требует дополнительной логики для инвалидации кеша
- Может показывать устаревшие данные
- Полезно для часто запрашиваемой статистики

### Подход 3: Оптимизация API-запросов

Можно распараллелить запросы для ускорения сбора:
- Требует изменения логики в `monthly-stats.js`
- Может увеличить нагрузку на API
- Не гарантирует укладывание в таймаут

## Дата исправления

2024-12-09

## Связанные файлы

- `pd.js` - основной файл бота, обработчики callback
- `monthly-stats.js` - модуль сбора месячной статистики
- `api.js` - модуль для работы с API

## Тестирование

Для проверки исправления:

1. Запустите бота: `npm start`
2. Нажмите кнопку "📅 Прошлый месяц" в меню статистики
3. Убедитесь, что:
   - Бот сразу отвечает на callback
   - Появляется сообщение о начале сбора
   - Обновляется прогресс обработки
   - После завершения приходит результат
   - Приложение не падает